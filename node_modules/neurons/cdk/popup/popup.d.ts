import { IPopupRef, IPopupOverlayRef, IPopupOption, IPopupManager, IPopupPanelRef, IToolTipRef, IToolTipOption } from './interfaces';
import { StateObject, BindingSelector, BindingTemplate, IUIStateStatic } from '../../binding/common/interfaces';
import { IEmitter } from 'neurons-emitter';
export declare class PopupRef<T extends StateObject> implements IPopupRef<T> {
    private _manager;
    private _container;
    isInternalPopup: boolean;
    constructor(_manager: IPopupManager, _container: HTMLElement, isInternalPopup?: boolean);
    private _nativeEmitter;
    option: IPopupOption<T>;
    overlay: IPopupOverlayRef<T>;
    panel: IPopupPanelRef<T>;
    onOpened: IEmitter<IPopupRef<T>>;
    onClosed: IEmitter<IPopupRef<T>>;
    onClose: IEmitter<IPopupRef<T>>;
    open(source: BindingSelector | BindingTemplate | HTMLElement | IUIStateStatic<T>, option?: IPopupOption<T>): void;
    getState(property?: string): any;
    setState(state: any): void;
    updatePosition(connectElement?: HTMLElement | MouseEvent): void;
    close(): void;
    protected _attachOverlay(container: HTMLElement, option?: IPopupOption<T>, isInternalPopup?: boolean): IPopupOverlayRef<T>;
    protected _attachPanel(container: HTMLElement, source: BindingSelector | BindingTemplate | HTMLElement | IUIStateStatic<T>, option?: IPopupOption<T>, isInternalPopup?: boolean): IPopupPanelRef<T>;
}
export declare class ToolTipRef implements IToolTipRef {
    private _manager;
    private _component;
    constructor(_manager: IPopupManager, _component: BindingSelector | BindingTemplate | HTMLElement | IUIStateStatic<any>, option?: IToolTipOption);
    private _option;
    private _ref;
    private _followMouse;
    private _delayTimeId;
    private _durationTimeId;
    private _opening;
    open(connectElement?: HTMLElement | MouseEvent): void;
    updateOption(option?: IToolTipOption): void;
    updatePosition(connectElement?: HTMLElement | MouseEvent): void;
    close(): void;
    private _mergeOption;
    private _unionTooltipClasses;
}
