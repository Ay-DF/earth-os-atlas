import { IDragScrollOption } from './interfaces';
export interface IDragScrollApi {
    shouldBeScroll(direction: string, start: MouseEvent, current: MouseEvent, distance?: number): any;
    shouldBeSkip(e: MouseEvent): any;
    markActive(e: MouseEvent): any;
    calcScrollDirection(container: HTMLElement, proxySelector?: string): string;
    scrollHorizontal(container: HTMLElement, offset: number, proxySelector?: string): any;
    scrollVertical(container: HTMLElement, offset: number, proxySelector?: string): any;
    naturalScrolling(container: HTMLElement, velocity: number[], proxySelector: string): any;
    attachStyle(container: any): any;
    restoreStyle(container: any): any;
}
export declare class DragScrollControler {
    enableDragScroll(container: HTMLElement, option: IDragScrollOption): void;
    disableDragScroll(container: HTMLElement): void;
    refreshDragScroll(container: HTMLElement): void;
    private _attachControl;
    private _detachControl;
    private _stopDragging;
    private _listenToDragScroll;
    private _createControlButtons;
    private _wrapApi;
    private _isEnabled;
    private _getOption;
}
