import { IEmitter } from 'neurons-emitter';
export declare type GridLayoutBox = {
    x: number;
    y: number;
    width: number;
    height: number;
};
export declare type GridRange = {
    x: number;
    y: number;
    width: number;
    height: number;
};
export declare type SizeAccessor<T> = (item: T) => {
    width: number;
    height: number;
};
export declare const defaultSizeAccessor: () => {
    width: number;
    height: number;
};
export interface IRowChangeEvent {
    type: 'add' | 'remove' | 'resize' | 'position';
    range: [number, number];
}
export interface IColChangeEvent {
    type: 'add' | 'remove' | 'resize' | 'position';
    range: [number, number];
}
export declare type IItemChangeEvent<T> = {
    colIndex: number;
    rowIndex: number;
    item: T;
}[];
/**
 * Grid matrix 栅格矩阵。
 * 类似表格，矩阵中同行元素或同列元素具有相同的尺寸，决定于同行或同列元素的最大尺寸。对于没有元素的cell以undefined作为占位值。
 * 一旦操作通过调用update接口更新行列元素偏移和尺寸变更。
 * @author AK
 */
export declare class GridMatrix<T> {
    private _minGridSize;
    private _size;
    private _id?;
    constructor(_minGridSize?: {
        width: number;
        height: number;
    }, _size?: SizeAccessor<T>, _id?: (item: T) => string);
    private _nativeEmitter;
    onRowChange: IEmitter<IRowChangeEvent>;
    onColChange: IEmitter<IColChangeEvent>;
    onItemAdded: IEmitter<IItemChangeEvent<T>>;
    onItemRemoved: IEmitter<IItemChangeEvent<T>>;
    private _rows;
    private _rowSizes;
    private _colSizes;
    private _dirtyIndex;
    private _gridSize;
    private _indexSize;
    private _itemToRow;
    destroy(): void;
    appendRow(): void;
    appendCol(): void;
    insertRow(rowIndex: any): void;
    insertColumn(colIndex: any): void;
    removeRow(rowIndex: any): void;
    removeCol(colIndex: any): void;
    isEmptyRow(rowIndex: any): boolean;
    isEmptyCol(colIndex: any): boolean;
    trimBelow(): void;
    trimRight(): void;
    put(colIndex: any, rowIndex: any, item: any): void;
    move(item: any, colIndex: any, rowIndex: any): void;
    add(colIndex: any, rowIndex: any, item: any): void;
    update(item: any): void;
    remove(item: any): [number, number];
    get(colIndex: any, rowIndex: any): T;
    index(item: any): [number, number];
    size(): {
        width: number;
        height: number;
    };
    count(): [number, number];
    getRowBox(rowIndex: any): GridLayoutBox;
    getColBox(colIndex: any): GridLayoutBox;
    getItemBox(colIndex: any, rowIndex: any): {
        x: any;
        y: any;
        width: any;
        height: any;
    };
    each(fn: (item: any, colIndex: any, rowIndex: any) => void): void;
    eachItems(range: GridRange, fn: (item: any, colIndex: any, rowIndex: any) => void): void;
    eachCells(range: GridRange, fn: (item: any, colIndex: any, rowIndex: any) => void): void;
    everyRowCells(rowIndex: any, fn: any): boolean;
    everyColCells(colIndex: any, fn: any): boolean;
    eachRowCells(rowIndex: any, fn: any): void;
    eachColCells(colIndex: any, fn: any): void;
    updatePosition(): void;
    private _setDirtyRow;
    private _setDirtyCol;
    private _update;
    private _remove;
    private _put;
    private _maxRowHeight;
    private _maxColWidth;
    private _removeRow;
    private _insertRow;
    private _removeCol;
    private _insertCol;
    private _deleteItemToRow;
    private _setItemToRow;
    private _getRow;
    private _emitRowChange;
    private _emitColChange;
}
