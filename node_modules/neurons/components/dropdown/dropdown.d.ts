import { IUIStateStatic, IBindingDefinition, IChangeDetector, BindingSelector, BindingTemplate } from '../../binding/common/interfaces';
import { ISVGIcon } from 'neurons-dom/dom/element';
import { IEmitter } from 'neurons-emitter';
import { ISelectionChangeEvent, IItemClickEvent, IItemStateStatic, IMultiSelectionChangeEvent } from '../interfaces';
import { IPopupOption, IPopupRef } from '../../cdk/popup/interfaces';
export interface IDropDownPopupOption<T> {
    component: BindingSelector | BindingTemplate | HTMLElement | IUIStateStatic<T>;
    option?: IPopupOption<T>;
}
export declare class DropDownButton<T> {
    dropdownPanel: BindingSelector | BindingTemplate | HTMLElement | IUIStateStatic<T>;
    dropdownPanelBinding: IBindingDefinition;
    dropdownPanelState: T;
    dropdownPosition: string;
    label: string;
    caretIcon: ISVGIcon;
    openFunction: (e: MouseEvent) => IDropDownPopupOption<T>;
    trigger: HTMLElement;
    changeDetector: IChangeDetector;
    panelOpened: IEmitter<IPopupRef<any>>;
    panelClosed: IEmitter<void>;
    opened: boolean;
    onClick(e: any): void;
}
export declare class DropDownList<T> {
    caretIcon: ISVGIcon;
    disabled: boolean;
    readonly: boolean;
    required: boolean;
    modalMode: boolean;
    active: boolean;
    searchableThreshold: number;
    enableSelection: boolean;
    enableMultiSelection: boolean;
    dataProvider: T[] | (() => T[]);
    selectedItem: T;
    selectedItems: T[];
    placeholder: string;
    labelField: string;
    labelFunction: any;
    itemRenderer: IItemStateStatic<T>;
    dropdownClass: string;
    dropdownOverlayClass: string;
    dropdownPosition: string;
    selectionChange: IEmitter<ISelectionChangeEvent<T>>;
    selectedItemChange: IEmitter<T>;
    multiSelectionChange: IEmitter<IMultiSelectionChangeEvent<T>>;
    selectedItemsChange: IEmitter<T[]>;
    itemClick: IEmitter<IItemClickEvent<T>>;
    trigger: HTMLElement;
    changeDetector: IChangeDetector;
    label: string;
    opened: boolean;
    invalid: boolean;
    onChanges(): void;
    onClick(e: any): void;
    openMultiSelectionList(): void;
    openSingleSelectctionList(): void;
    getItemLabel(): any;
    protected _getListTemplate(binding: any): string;
    protected _getSearchableListTemplate(binding: any): string;
    private _isSearchable;
    private _getItemLabel;
    private _isInvalid;
}
