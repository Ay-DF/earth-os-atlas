import { Input } from './input';
export declare class TextArea extends Input {
    resize: 'none' | 'auto' | 'horizontal' | 'vertical';
    autoHeight: boolean;
    onChanges(changes: any): void;
    onInputChange(e: KeyboardEvent): void;
    protected calcAutoHeight(): void;
}
