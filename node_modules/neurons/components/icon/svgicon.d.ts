import { ISVGIcon } from 'neurons-dom/dom/element';
import { StateChanges } from '../../binding/common/interfaces';
export interface ISvgIconState {
    icon: ISVGIcon;
}
export declare class SvgIcon implements ISvgIconState {
    icon: ISVGIcon;
    prefix: string;
    iconName: string;
    iconWidth: number;
    iconHeight: number;
    iconLigatures: any[];
    iconUnicode: string;
    iconPath: string;
    onChanges(changes: StateChanges): void;
}
