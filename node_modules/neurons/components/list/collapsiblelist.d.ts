import { IEmitter } from 'neurons-emitter';
import { StateChanges, IBindingRef } from '../../binding/common/interfaces';
import { ISelectionChangeEvent, IItemStateStatic, IItemClickEvent, IMultiSelectionChangeEvent } from '../interfaces';
import { IPopupRef } from '../../cdk/popup/interfaces';
export declare class ItemRendererWrapper<T> {
    item: T;
    itemIndex: number;
    selected: boolean;
    label: string;
    itemRenderer: IItemStateStatic<T>;
    params: any;
    container: HTMLElement;
    itemClick: IEmitter<{
        item: T;
        index: number;
        causeEvent: MouseEvent;
    }>;
    private _ref;
    private _oldSize;
    onChanges(changes: any): void;
    onItemClick(e: any): void;
}
export declare class CollapsibleList<T> {
    active: boolean;
    enableSelection: boolean;
    enableMultiSelection: boolean;
    listStyle: 'single-line' | 'multi-line';
    dataProvider: T[];
    selectedItem: T;
    selectedItems: T[];
    labelField: string;
    labelFunction: (item: T) => string;
    itemRenderer: IItemStateStatic<T>;
    itemRendererParams: any;
    selectionChange: IEmitter<ISelectionChangeEvent<T>>;
    selectedItemChange: IEmitter<T>;
    multiSelectionChange: IEmitter<IMultiSelectionChangeEvent<T>>;
    selectedItemsChange: IEmitter<T[]>;
    itemClick: IEmitter<IItemClickEvent<T>>;
    shim: HTMLElement;
    container: HTMLElement;
    content: HTMLElement;
    moreDataProvider: T[];
    itemRefs: IBindingRef<any>[];
    moreRef: IBindingRef<any>;
    popupRef: IPopupRef<any>;
    onChanges(changes: StateChanges): void;
    onDestroy(): void;
    onResize(): void;
    getItemLabel(item: T): string;
    getItemParams(item: T, index: number): any;
    isItemSelected(item: T, index: number): boolean;
    onItemClick(e: MouseEvent, item: T, index: number): void;
    protected applySingleSelection(item: T, index: number): void;
    protected applyMultiSelection(item: T, index: number): void;
    protected createItemRenderer(item: T, index: number): IBindingRef<any>;
    protected wrapItemState(item: T, index: number): {
        'itemRenderer': IItemStateStatic<T>;
        'item': T;
        'itemIndex': number;
        'selected': boolean;
        'label': string;
        'params': any;
    };
    private _reset;
    private _sliceDataProvider;
    private _calcCollapsible;
    private _createMoreButton;
    private _showMore;
    private _createMoreState;
}
