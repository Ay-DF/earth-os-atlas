import { IEmitter } from 'neurons-emitter';
import { ISVGIcon } from 'neurons-dom/dom/element';
export declare class CheckItem {
    readonly: boolean;
    disabled: boolean;
    checked: boolean;
    checkMode: 'multi' | 'single';
    checkStyle: 'checkbox' | 'radio' | 'toggle' | 'check' | 'highlight' | 'background' | 'capsule' | 'edge' | 'v-edge';
    checkPosition: 'before' | 'after';
    checkAlign: 'left' | 'right' | 'justify';
    container: HTMLElement;
    iconDom: HTMLElement;
    checkedChange: IEmitter<boolean>;
    change: IEmitter<boolean>;
    checkIcon: ISVGIcon;
    uncheckIcon: ISVGIcon;
    icon: any;
    iconWidth: string;
    iconLeft: string;
    iconRight: string;
    contentLeft: string;
    contentRight: string;
    onChanges(changes: any): void;
    onClicked(e: MouseEvent): void;
}
