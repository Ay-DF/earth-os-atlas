import { ListBase } from '../list/listbase';
import { IItemState, IItemStateStatic } from '../interfaces';
import { IChangeDetector } from '../../binding/common/interfaces';
export declare class HorizontalCheckGroupItemWrapper implements IItemState<any> {
    item: any;
    itemIndex: number;
    selected: boolean;
    label: string;
    params: any;
}
export declare class HorizontalCheckGroup<T> extends ListBase<T> {
    required: boolean;
    checkStyle: 'checkbox' | 'radio' | 'check' | 'highlight' | 'background' | 'capsule' | 'tag' | 'edge' | 'v-edge';
    checkPosition: 'before' | 'after';
    itemRenderer: IItemStateStatic<T>;
    cdr: IChangeDetector;
    invalid: boolean;
    onChanges(changes: any): void;
    getItemParams(item: T, index: number): any;
    protected applySingleSelection(item: T, index: number): void;
    protected applyMultiSelection(item: T, index: number): void;
    private _isInvalid;
}
