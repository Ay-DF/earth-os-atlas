import { IInjector } from "neurons-injector";
import { IBindingRef } from "../../binding/common/interfaces";
import { IJSEFormControl, IJSENode } from "./interfaces";
export declare class JSERendererFactory {
    protected renderers: {
        [renderer: string]: any;
    };
    constructor(renderers: {
        [renderer: string]: any;
    });
    reset(renderers: {
        [renderer: string]: any;
    }): void;
    getRendererType(node: IJSENode): string;
    getRenderer(node: IJSENode): any;
    getInstance(node: IJSENode, container: HTMLElement, formControl: IJSEFormControl, injector: IInjector, extraHostBinding?: {
        [key: string]: string;
    }, extraState?: any): IBindingRef<any>;
    private _getRendererType;
}
