import { IMultiSelectionChangeEvent, IItemState, IItemStateStatic } from '../interfaces';
import { IEmitter } from 'neurons-emitter';
export declare class CheckBoxGroupItem<T> implements IItemState<T> {
    item: T;
    itemIndex: number;
    selected: boolean;
    label: string;
}
export declare class CheckBoxGroup<T> {
    dataProvider: T[];
    selectedItems: T[];
    labelField: string;
    labelFunction: (item: T) => string;
    itemRenderer: IItemStateStatic<T>;
    active: boolean;
    selectedItemsChange: IEmitter<T>;
    multiSelectionChange: IEmitter<IMultiSelectionChangeEvent<T>>;
}
