import { IChangeDetector, IBindingRef } from '../../binding/common/interfaces';
import { ISVGIcon } from 'neurons-dom/dom/element';
import { IEmitter } from 'neurons-emitter';
import { IPopupOption, IPopupRef } from '../../cdk/popup/interfaces';
import { ObservableLike } from 'neurons-utils/utils/asyncutils';
export interface IPopupButtonOption extends IPopupOption<any> {
}
export declare class PopupButton {
    disabled: boolean;
    color: 'basic' | 'primary';
    mode: 'basic' | 'raised' | 'stroked' | 'flat' | 'simulated';
    caretPosition: 'before' | 'after';
    caretIcon: ISVGIcon;
    invalid: boolean;
    readonly: boolean;
    hideLabel: boolean;
    label: string;
    placeholder: string;
    popupOption: IPopupButtonOption;
    openFunction: (container: HTMLElement, popupRef: IPopupRef<any>) => IBindingRef<any> | Promise<IBindingRef<any>> | ObservableLike<IBindingRef<any>>;
    opened: IEmitter<void>;
    closed: IEmitter<void>;
    changeDetector: IChangeDetector;
    _opened: boolean;
    private _popupRef;
    private _destroyed;
    onChanges(changes: any): void;
    onDestroy(): void;
    onClick(e: MouseEvent): void;
}
