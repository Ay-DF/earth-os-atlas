import { ISVGIcon } from 'neurons-dom/dom/element';
import { IChangeDetector } from '../../binding/common/interfaces';
import { ObservableLike } from 'neurons-utils/utils/asyncutils';
export declare class ImageUploader {
    url: string;
    accept: string;
    label: string;
    hint: string;
    iconPlaceholder: ISVGIcon;
    uploadApi: (input: HTMLInputElement) => Promise<string> | ObservableLike<string>;
    uploadInput: HTMLInputElement;
    cdr: IChangeDetector;
    uploading: boolean;
    uploadError: boolean;
    errorMessage: string;
    onInit(): void;
}
