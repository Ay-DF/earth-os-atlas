import { ISVGIcon } from 'neurons-dom/dom/element';
import { IEmitter } from 'neurons-emitter';
import { IChangeDetector } from '../../binding/common/interfaces';
import { IHttpProgress } from '../../helper/httpclient';
import { ObservableLike } from 'neurons-utils/utils/asyncutils';
export declare class FileUploadTrigger {
    accept: string;
    label: string;
    icon: ISVGIcon;
    uploadApi: (input: HTMLInputElement) => Promise<string> | ObservableLike<string> | IHttpProgress;
    begin: IEmitter<HTMLInputElement>;
    completed: IEmitter<any>;
    failed: IEmitter<any>;
    canceled: IEmitter<void>;
    uploadInput: HTMLInputElement;
    cdr: IChangeDetector;
    uploading: boolean;
    onInit(): void;
}
