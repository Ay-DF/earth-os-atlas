import { IEmitter } from 'neurons-emitter';
import { ISelectionChangeEvent, IItemStateStatic, IItemState } from '../interfaces';
export declare class RadioGroupItem<T> implements IItemState<T> {
    item: T;
    itemIndex: number;
    selected: boolean;
    label: string;
}
export declare class RadioGroup<T> {
    dataProvider: T[];
    selectedItem: T;
    labelField: string;
    labelFunction: (item: T) => string;
    itemRenderer: IItemStateStatic<T>;
    active: boolean;
    selectedItemChange: IEmitter<T>;
    selectionChange: IEmitter<ISelectionChangeEvent<T>>;
}
