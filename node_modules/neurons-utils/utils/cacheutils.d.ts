/**
 * Window or Global or {};
 */
export declare const globalContext: any;
/**
 * example:
 * ```
 * const obj = globalCache('someName', { });
 * ```
 * or
 * ```
 * const obj = globalCache('someName', () => {
 *  return {}
 * });
 * ```
 */
export declare const globalCache: any;
export interface ILimitedDictionary<T> {
    has: (key: string) => boolean;
    get: (key: string) => T;
    set: (key: string, value: T) => void;
    find: (fn: ((key: string, value: T) => boolean)) => [string, T];
}
export declare function globalLimitedDictionary<T>(key: string): ILimitedDictionary<T>;
